# ── 编译产物 ───────────────────────────────────────────────
/build/
/build-cpu/
*.o
*.a
*.so

# ── 模型权重 / 大文件（不入云端代码库）───────────────────
*.gguf
*.safetensors
*.bin
checkpoints/
*.jpg
*.jpeg
*.png
*.mp4

# ── 评估输出（生成物，不入库）─────────────────────────────
outputs/

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── 临时 / 日志 ───────────────────────────────────────────
*.log
core
core.*

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── 环境变量 / 密钥 ───────────────────────────────────────
.env
.env.*
