# ── 编译产物 ───────────────────────────────────────────────
/build/
/build-cpu/
/build-cross/
/build-x86-ref/
*.o
*.a
*.so
/models/
# ── 模型权重 / 大文件（不入云端代码库）───────────────────
*.gguf
*.safetensors
*.bin
checkpoints/
*.jpg
*.jpeg
*.mp4

# ── 评估输出（生成物，不入库）─────────────────────────────
outputs/
eval-outputs/*

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── 临时 / 日志 ───────────────────────────────────────────
*.log
core
core.*

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.claude/

# ── 环境变量 / 密钥 ───────────────────────────────────────
.env
.env.*

# ── LaTeX 编译中间产物（docs/paper/*.pdf 保留入库）────────
*.aux
*.out
*.toc
*.lof
*.lot
*.nav
*.snm
*.bbl
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk

local/*
